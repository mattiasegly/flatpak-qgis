name: tcl
sources:
  - type: git
    url: https://github.com/tcltk/tcl.git
    tag: core-8-6-14
    commit: 16b5e6486a8e0e1746e35a0a88125ed72e286cd9
    x-checker-data:
      type: git
      tag-pattern: core-(\d+-\d+-\d+)
buildsystem: simple
builddir: true
subdir: unix
build-commands:
  - ./configure --prefix=/app --enable-64bit
  - make
  - make install
post-install:
  - chmod 755 /app/lib/libtcl*.so
