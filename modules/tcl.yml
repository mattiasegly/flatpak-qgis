name: tcl
sources:
  - type: git
    url: https://github.com/tcltk/tcl.git
    tag: core-8-6-14
    commit: 89b41e0a93ac87b8b838cf82db3da1d6085623c4
    x-checker-data:
      type: git
      tag-pattern: core-(\d+-\d+-\d+)
buildsystem: simple
builddir: true
subdir: unix
build-commands:
  - ./configure --prefix=/app --enable-64bit
  - make
  - make install
post-install:
  - chmod 755 /app/lib/libtcl*.so
