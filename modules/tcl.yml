name: tcl
sources:
  - type: git
    url: https://github.com/tcltk/tcl.git
    tag: core-8-6-14
    commit: 1d84e70c2decaf650f0f1a41b3de81bb0e19b58e
    x-checker-data:
      type: git
      tag-pattern: core-(\d+-\d+-\d+)
buildsystem: simple
builddir: true
subdir: unix
build-commands:
  - ./configure --prefix=/app --enable-64bit
  - make
  - make install
post-install:
  - chmod 755 /app/lib/libtcl*.so
