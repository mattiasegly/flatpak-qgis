name: sqlite
sources:
  - type: git
    url: https://github.com/sqlite/sqlite.git
    tag: version-3.43.2
    commit: 43fa051ec7c5a7d89e1f5f541a5cd34d53e07422
    x-checker-data:
      type: git
      tag-pattern: version-(\d+\.\d+\.\d+)
buildsystem: simple
builddir: true
build-commands:
  - CFLAGS="-DSQLITE_ENABLE_COLUMN_METADATA=1" ./configure --prefix=/app --disable-static
    --enable-all                                                                                       #enable-all is only; fts4/5 geopoly rtree session
  - make
  - make install
