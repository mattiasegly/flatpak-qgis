name: minizip
sources:
  - type: git
    url: https://github.com/madler/zlib.git
    tag: v1.2.13
buildsystem: simple
builddir: true
subdir: contrib/minizip
build-commands:
  - rm -f Makefile
  - autoreconf -fiv
  - ./configure --prefix=/app
  - make
  - make install
